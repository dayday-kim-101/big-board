# 복사해서 .dev.vars 로 사용하세요 (.dev.vars 는 gitignore됨)
GITHUB_TOKEN="github_pat_xxx"
GITHUB_REPO="owner/repo"
GITHUB_BRANCH="develop"

# 재료정리 일별 수집(scripts/jaelyo-snapshot.mjs)은 네이버 공개 데이터라 인증·환경변수가 필요 없다.
# 네이버 API는 당일만 제공 → 과거 백필 불가. 그냥: node scripts/jaelyo-snapshot.mjs
